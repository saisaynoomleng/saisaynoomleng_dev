import { Button } from '#components/ui/button';
import React from 'react';

export const Bounded = (): React.JSX.Element => {
  return (
    <div>
      Bounded
      <Button>Click</Button>
    </div>
  );
};
